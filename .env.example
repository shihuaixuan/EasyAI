# EasyAI 环境变量配置示例
# 复制此文件为 .env 并填入你的实际配置值

# =============================================================================
# 配置管理系统
# =============================================================================
# 配置文件路径（可选，默认为项目根目录下的config目录）
CONFIG_PATH=./config

# 用户配置目录（可选，默认为用户家目录下的.easyai）
USER_CONFIG_DIR=~/.easyai

# 缓存过期时间（可选，秒，默认3600秒即1小时）
CONFIG_CACHE_TTL=3600

# 是否启用配置缓存（可选，默认true）
ENABLE_CONFIG_CACHE=true

# =============================================================================
# OpenAI 配置
# =============================================================================
# OpenAI API密钥（必需）
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI 组织ID（可选）
OPENAI_ORGANIZATION=org-your-organization-id

# OpenAI 项目ID（可选）
OPENAI_PROJECT=proj_your-project-id

# OpenAI 基础URL（可选，默认为官方API）
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI 默认模型（可选）
OPENAI_MODEL=gpt-3.5-turbo

# OpenAI 默认温度（可选）
OPENAI_TEMPERATURE=0.7

# OpenAI 默认最大令牌数（可选）
OPENAI_MAX_TOKENS=2048

# OpenAI 超时时间（可选，秒）
OPENAI_TIMEOUT=30

# OpenAI 最大重试次数（可选）
OPENAI_MAX_RETRIES=3

# =============================================================================
# DeepSeek 配置
# =============================================================================
# DeepSeek API密钥（必需）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# DeepSeek 基础URL（可选，默认为官方API）
DEEPSEEK_BASE_URL=https://api.deepseek.com

# DeepSeek 默认模型（可选）
DEEPSEEK_MODEL=deepseek-chat

# DeepSeek 默认温度（可选）
DEEPSEEK_TEMPERATURE=0.7

# DeepSeek 默认最大令牌数（可选）
DEEPSEEK_MAX_TOKENS=2048

# DeepSeek 超时时间（可选，秒）
DEEPSEEK_TIMEOUT=30

# DeepSeek 最大重试次数（可选）
DEEPSEEK_MAX_RETRIES=3

# =============================================================================
# Claude (Anthropic) 配置
# =============================================================================
# Anthropic API密钥（必需）
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Anthropic 基础URL（可选，默认为官方API）
ANTHROPIC_BASE_URL=https://api.anthropic.com

# Claude 默认模型（可选）
CLAUDE_MODEL=claude-3-sonnet-20240229

# Claude 默认温度（可选）
CLAUDE_TEMPERATURE=0.7

# Claude 默认最大令牌数（可选）
CLAUDE_MAX_TOKENS=2048

# Claude API版本（可选）
CLAUDE_ANTHROPIC_VERSION=2023-06-01

# Claude 超时时间（可选，秒）
CLAUDE_TIMEOUT=30

# Claude 最大重试次数（可选）
CLAUDE_MAX_RETRIES=3

# =============================================================================
# Gemini 配置
# =============================================================================
# Google AI API密钥（必需）
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini 基础URL（可选）
GEMINI_BASE_URL=https://generativelanguage.googleapis.com

# Gemini 默认模型（可选）
GEMINI_MODEL=gemini-pro

# Gemini 默认温度（可选）
GEMINI_TEMPERATURE=0.7

# Gemini 默认最大令牌数（可选）
GEMINI_MAX_TOKENS=2048

# Gemini 超时时间（可选，秒）
GEMINI_TIMEOUT=30

# Gemini 最大重试次数（可选）
GEMINI_MAX_RETRIES=3

# =============================================================================
# 通义千问 (Qwen) 配置
# =============================================================================
# 阿里云API密钥（必需）
QWEN_API_KEY=your-qwen-api-key-here

# 通义千问基础URL（可选）
QWEN_BASE_URL=https://dashscope.aliyuncs.com/api/v1

# 通义千问默认模型（可选）
QWEN_MODEL=qwen-turbo

# 通义千问默认温度（可选）
QWEN_TEMPERATURE=0.7

# 通义千问默认最大令牌数（可选）
QWEN_MAX_TOKENS=2048

# 通义千问超时时间（可选，秒）
QWEN_TIMEOUT=30

# 通义千问最大重试次数（可选）
QWEN_MAX_RETRIES=3

# =============================================================================
# 智谱AI (ZhipuAI) 配置
# =============================================================================
# 智谱AI API密钥（必需）
ZHIPU_API_KEY=your-zhipu-api-key-here

# 智谱AI基础URL（可选）
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# 智谱AI默认模型（可选）
ZHIPU_MODEL=glm-4

# 智谱AI默认温度（可选）
ZHIPU_TEMPERATURE=0.7

# 智谱AI默认最大令牌数（可选）
ZHIPU_MAX_TOKENS=2048

# 智谱AI超时时间（可选，秒）
ZHIPU_TIMEOUT=30

# 智谱AI最大重试次数（可选）
ZHIPU_MAX_RETRIES=3

# =============================================================================
# 应用配置
# =============================================================================
APP_NAME=EasyAI
APP_VERSION=1.0.0
ENVIRONMENT=development
DEBUG=true
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 服务器配置
HOST=0.0.0.0
PORT=8000
WORKERS=1
RELOAD=true

# 数据库配置
DATABASE_URL=postgresql://username:password@localhost:5432/easyai
DATABASE_ECHO=false
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Redis配置
REDIS_URL=redis://localhost:6379/0
REDIS_PASSWORD=
REDIS_DB=0
REDIS_MAX_CONNECTIONS=10

# JWT配置
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# 密码加密配置
PASSWORD_HASH_ALGORITHM=bcrypt
PASSWORD_HASH_ROUNDS=12

# LangChain配置
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=your-langchain-api-key-here
LANGCHAIN_PROJECT=easyai

# 向量数据库配置
VECTOR_DB_TYPE=chroma
CHROMA_HOST=localhost
CHROMA_PORT=8000
CHROMA_COLLECTION_NAME=easyai_documents

# 文件存储配置
FILE_STORAGE_TYPE=local
FILE_STORAGE_PATH=./storage
FILE_MAX_SIZE=10485760  # 10MB
FILE_ALLOWED_EXTENSIONS=.txt,.pdf,.docx,.md,.json

# 日志配置
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_FILE_PATH=./logs/app.log
LOG_FILE_MAX_SIZE=10485760  # 10MB
LOG_FILE_BACKUP_COUNT=5
LOG_ROTATION=daily

# Celery配置
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_ACCEPT_CONTENT=json
CELERY_TIMEZONE=UTC
CELERY_ENABLE_UTC=true

# 监控配置
MONITORING_ENABLED=false
PROMETHEUS_PORT=9090
METRICS_ENDPOINT=/metrics

# 邮件配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-email-password
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=noreply@easyai.com
SMTP_FROM_NAME=EasyAI

# 缓存配置
CACHE_TTL=3600  # 1小时
CACHE_MAX_SIZE=1000
CACHE_KEY_PREFIX=easyai:

# API限流配置
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST=10

# 安全配置
SECURITY_HEADERS_ENABLED=true
CSRF_PROTECTION_ENABLED=true
XSS_PROTECTION_ENABLED=true

# 开发配置
DEV_RELOAD=true
DEV_DEBUG_TOOLBAR=true
DEV_PROFILER_ENABLED=false

# 测试配置
TEST_DATABASE_URL=sqlite:///./test.db
TEST_REDIS_URL=redis://localhost:6379/15